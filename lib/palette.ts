import { colors } from '@/lib/design-tokens'

export const CATEGORY_PALETTE = [
  colors.plant,
  colors.olive,
  colors.plantDark,
  colors.oliveLight,
  colors.beigeDark,
  colors.accentPink,
  colors.plantLight,
  colors.oliveDark,
]
